# Bundled tunnel sidecars

Place the Cloudflare Tunnel executable here when packaging the desktop app:

- Windows: `bin/cloudflared.exe`
- macOS/Linux: `bin/cloudflared`

The server will prefer this bundled sidecar over `localtunnel`.
